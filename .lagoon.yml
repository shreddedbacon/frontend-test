docker-compose-yaml: docker-compose.yml
environments:
  main:
    routes:
      - nginx:
        - "frontend-test.test6.shreddedbacon.com":
            pathRoutes:
              - toService: backend
                path: /api
              - toService: backend
                path: /cms

routes:
  autogenerate:
    pathRoutes:
      - fromService: nginx
        toService: backend
        path: /api
      - fromService: nginx
        toService: backend
        path: /cms