services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    labels:
      lagoon.type: nginx
    ports:
      - "8080:8080"
  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    labels:
      lagoon.type: nginx
    ports:
      - "8080:8080"