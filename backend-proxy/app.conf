server {

    listen 8080 default_server;

    include /etc/nginx/helpers/*.conf;

    location = / {
        proxy_pass http://nginx.backend-test-main.svc:8080;
    }

    # location = /cms {
    #     proxy_pass http://nginx.backend-test-main.svc:8080;
    # }

    # location = /api {
    #     proxy_pass http://nginx.backend-test-main.svc:8080;
    # }
}